# Data notes

`data/` contains the unfinished Senate transcript acquisition and processing
work for this paused project. Keep it outside Git. Its canonical working copy
is `OneDrive/_working-data/ar_congress/data`, linked here through
`~/PARA/_working-data` so that project code can continue using `data/...`.

Before resuming on a new computer, confirm that `~/PARA/_working-data` links
to that computer's local OneDrive sync folder. Do not promote this incomplete
work to Resources.
